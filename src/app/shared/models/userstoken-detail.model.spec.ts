import { UserstokenDetail } from './userstoken-detail.model';

describe('UserstokenDetail', () => {
  it('should create an instance', () => {
    expect(new UserstokenDetail()).toBeTruthy();
  });
});
