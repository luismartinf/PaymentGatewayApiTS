import { UsersloginDetail } from './userslogin-detail.model';

describe('UsersloginDetail', () => {
  it('should create an instance', () => {
    expect(new UsersloginDetail()).toBeTruthy();
  });
});
