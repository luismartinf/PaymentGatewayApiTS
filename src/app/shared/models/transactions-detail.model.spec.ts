import { TransactionsDetail } from './transactions-detail.model';

describe('TransactionsDetail', () => {
  it('should create an instance', () => {
    expect(new TransactionsDetail()).toBeTruthy();
  });
});
